# Map: Thither v1 implementation

Label: `wayfinder:map`
Tickets: `.scratch/thither-v1/issues/`

## Destination

A deployed, self-contained `index.html` on GitHub Pages that implements everything specified in [docs/dsl.md](../../docs/dsl.md) and [docs/browser-client.md](../../docs/browser-client.md), backed by a spec-derived Vitest conformance suite, and driven from a real browser search shortcut.

Done when: the language core passes conformance tests traceable to every normative rule in `dsl.md`; the client implements the full execution flow, versioned persistence, bounded history, and fallback UI of `browser-client.md`; the build emits only `index.html`; the page is live on Pages; and the shortcut has been hand-verified against the checklist.

## Notes

**This map carries execution.** Wayfinder's plan-only default is overridden: build tickets here produce working code, not just decisions. The specs are already `Status: agreed`, so the remaining unknowns are implementation-shaped.

**Domain and sources.** [CONTEXT.md](../../CONTEXT.md) is the glossary — use its terms (dimension, target, destination template, focus, match, argument balance) in ticket titles, test names, and identifiers, and avoid the terms it lists under `_Avoid_`. `docs/dsl.md` and `docs/browser-client.md` are normative. ADR-0001 (localStorage only), ADR-0002 (single-file on Pages), ADR-0003 (resume from last stack value), and ADR-0004 (publish by `v*` tag, not by merge) bind implementation choices. If a ticket's outcome contradicts a spec or ADR, say so explicitly and amend the doc or write a new ADR — never let the code silently drift from the spec.

**Skills each session should consult.** `grilling` + `domain-modeling` for any decision ticket; `codebase-design` for the interface-design tickets (04, 16); `tdd` for every build ticket; `prototype` for UI shape questions.

**Standing decisions from charting** (these are settled; don't reopen them as tickets):

- **Stage spine.** S0 Scaffold → S1 Core → S2 Walking skeleton → S3 Single-file bundle and live on Pages → S4 Full persistence → S5 Full UI → S6 Cross-browser verification and release. Deployment lands early, at S3, because ADR-0002's single-file constraint and the `%s` shortcut contract are only truly testable on a deployed page.
- **Every stage ends with a HITL checkpoint ticket** that blocks the next stage. Checkpoints exist especially where a new interface was designed.
- **Test convention.** Table-driven fixture suites whose cases name the `dsl.md` section they enforce (e.g. `§4.4 inferred separator: longest matching prefix`), plus the worked programs of `dsl.md` §7 as golden end-to-end cases. No separate traceability matrix — it would rot.
- **Types before tests.** The core's TypeScript value model is designed and written down (ticket 04) before the first core test is written.
- **Scheme policy is closed.** No client-side scheme allowlist and no scheme-based confirmation, exactly as `browser-client.md` documents. Thither is a power-user tool whose users author their own targets; `javascript:` destinations are accepted. Not revisited at S3.

**Ticket protocol.** One ticket per session. Set `Status: claimed` before any work. Resolve by appending an `## Answer` section, setting `Status: resolved`, and adding a one-line gist to Decisions so far below.

## Decisions so far

<!-- one line per resolved ticket: gist + link -->

- [Code principles and style rules](issues/01-code-principles-and-style-rules.md): Biome (2 spaces, width 100) for format and lint, strict tsc without `erasableSyntaxOnly` so enums stay, `type`/`interface` over classes, `src/dsl` + `src/lib` + `src/client` with a DOM-free tsconfig for the core and dependency-cruiser holding the import direction, values-not-exceptions in the core behind one `invariant()`, `readonly` plus test-only deep freeze, tests in `tests/` subfolders citing spec sections, `ticket/NN-slug` branches merged `--no-ff`. Written to [docs/code-standards.md](../../docs/code-standards.md).
- [S0 checkpoint: the harness is trustworthy](issues/03-s0-checkpoint.md): harness confirmed green for S1. Kept the bleeding-edge majors (TS7/Vite8/Vitest5). Moved the pi coding agent out of `package.json` into `devenv.nix` `enterShell` (repo-local pnpm global prefix) so it lives in the dev shell but CI never pulls it — `--no-optional` was rejected because it would also drop `@swc/*` native binaries `lint:boundaries` needs. Pre-commit gate kept as-is. Amended [docs/code-standards.md](../../docs/code-standards.md) with a "How the boundaries are enforced" note (two-tsconfig DOM-free mechanism + `@swc/core` parser workaround). CI pnpm-version resolution (`devEngines` set but no top-level `packageManager` field) deferred to the human to verify on a real runner.
- [Scaffold the toolchain](issues/02-toolchain-scaffold.md): pnpm project on exactly-pinned Vite 8 / Vitest 5 / TypeScript 7 / Biome 2.5 / dependency-cruiser 18 / `fzf@0.5.2`, `src/{dsl,lib,client}` with the four-symbol `src/dsl/index.ts` and the `invariant()` helper, two tsconfigs (the dsl one DOM-free), Husky + lint-staged and a CI workflow running `check → typecheck → lint:boundaries → test → build`, plus a Pages deploy workflow authored but not enabled, triggered by a `v*` tag rather than a merge ([ADR 0004](../../docs/adr/0004-publish-by-tag.md)). `@swc/core` was added as dependency-cruiser's TypeScript parser, since under TypeScript 7 it cruised zero modules and passed vacuously. DOM-free core, both boundary rules (runtime *and* type-only imports), and a deliberate type error were each observed failing and then passing.
- [Design the core type model and its seams](issues/04-core-type-model-and-seams.md): committed the whole type algebra to `src/dsl/types.ts` — `Dim`/`NormDim` and `Template`/`Url` branded (normalization is positional; `Url ⊆ Template`), envelopes wire-shaped (`["sigil", body]`, in-memory === persisted JSON), `Program` one flat sigil-tuple union, unknown-field preservation narrowed to `E` only. **Redesigned the public interface** ([ADR 0005](../../docs/adr/0005-extensible-interpreter-environment.md)): the operation set is a client-extensible `InterpreterEnv`, `defaultEnv()` → `createInterpreter(env)` → an interpreter carrying `emptyProgram`/`initialStack`/`append`/`execute`; operations are `(stack) => stack` mutating in place, dispatched by name via `env.symbols` (the extensibility *and* parallelism seam); `parse`→`append`, free `initialStack()` dropped. Amended `dsl.md` §2, `browser-client.md`, `code-standards.md`. Recorded the module/seam map and parallel waves (05/06/08 → 07 → 09 → 10/11 → 12/13). Re-wired the error/unwind primitives from 13 into 09 (operations fail *through* them); updated tickets 05/09/13/14.
- [Parse string tokens into values](issues/05-token-parsing.md): implemented the total string-token half of `append` as `parseToken(token, operations)` in `src/dsl/lib/token-parsing.ts` (type-only import of `types.ts`, a DOM-free leaf). First-match decision order settles totality: standalone `.` → separator; `..`-prefix → strip exactly one dot to a literal, never re-interpreted (`..$`→literal `.$`, `..`→`.`, `...`→`..`); single leading dot → `["op", name]` iff the env's operation set binds it, else a locally-constructed `parse_error` `E`; everything else → a verbatim literal (`node.js`, URLs, `{}` templates accumulate like dimensions). The `..$`-vs-`.$` distinction is carried by the position-0 sigil (`lit` vs `op`), which is what the ticket-09 lifecycle check reads. Literals minted as raw `Dim` here; destination validation is deliberately deferred to ticket 06. Recognition is env-relative (ADR-0005), proven with empty/extended operation sets. 44 tests green; `verify` clean; unblocks 09 jointly with 07.
- [Validate URLs and destination templates](issues/06-destination-validation.md): implemented the `destination` seam as `src/dsl/lib/destination.ts` - `validateDestination(raw)` renders every `{}` to the probe `thither` then `new URL(...)`; a successful parse *is* dsl.md §2's "parses + explicit scheme" check (the single-arg WHATWG constructor rejects relative refs), and on success the **byte-identical original** is returned branded, error path left to the caller (`.set` -> `invalid_destination`, supplied `S` -> parse-time `E`). `countPlaceholders` owns the count `P` for argDelta. **Design point to note:** the core omits the DOM lib, so `URL`'s type is supplied by a *module-scoped* `declare const URL` - the WHATWG `URL` is a universal platform global, not a client-only API, so this doesn't breach ADR-0001 and keeps localStorage/document/window still type-errors; recorded as a standing rule in `docs/code-standards.md` (universal globals re-supplied via a module-scoped `declare const`, never by widening the core `lib`). 74 tests green; boundaries clean; unblocks 07 and feeds 10/12.
- [Validate and normalize supplied structured values](issues/07-structured-value-validation.md): implemented the structured-value half of parse in `src/dsl/lib/dimension.ts` (renamed from the `normalization` module now that it validates too; dimension **validation and normalization split as separate concerns** so normalization can't fail: `isValidDimension` is the parser's whitespace gate; `normalizeDimension`/`normalizeDimensions` are total transforms - trim -> locale-independent lowercase -> dedupe -> UTF-16 sort, diacritics preserved; its own file so ops 10/11 import it alone) and `src/dsl/lib/structured-value.ts` (`validateStructured` dispatching on the `["S"|"R"|"E", body]` sigil). Every parse-time rejection is `parse_error` (§6 phase rule); a valid supplied `E` returns as itself. `S` fully validated/normalized (reuses ticket 06 for destinations, canonical-JSON key for the unique-dimension-set rule, target order preserved, unknown fields dropped); `E` held to §6's weaker rule (string type/description, type unchecked, extra fields preserved); `R` **shape-checked only** - no normalization, no destination re-validation, inputs verbatim - because a produced `R` holds partially-rendered destinations (§5). **Two spec-alignment fixes this session:** `ErrorType` corrected to §6's four types (separate commit `fix: align ErrorType…`, dropping the stale `invalid_value`/`missing_dimensions`/`invalid_stack`), and ticket 07's body reworded off `invalid_value` onto the phase rule. 119 tests green; boundaries clean. Unblocks 09 (with 05).
- [Build the fuzzy matcher adapter](issues/08-matcher-adapter.md): implemented `src/dsl/lib/matcher.ts` over pinned `fzf@0.5.2` - `match(targets, queryDimensions: readonly Dim[])` matches each dimension independently against each target's space-joined searchable string, keeps targets matched by *every* dimension (AND), sums scores, unions positions (ascending/deduped), returns in target-set order. **Seam refined in review:** the matcher takes a single flat query list and has **no `focus` param** (matching has no notion of focus; combining focus with the explicit literals and the focus-vs-explicit distinction move up to the operations, tickets 10/11) and **normalizes its own input** via the normalization seam (idempotent, so callers may pass raw `Dim` or already-normalized dims freely) - this refines ticket 04's seam row so the matcher owns *matching*, not query construction. One `Fzf` per dimension over target *objects* (selector, not raw strings, so shared searchable strings don't collide); empty query short-circuits to all-targets/score-0/no-positions. **Explicit config, justified on the ticket:** `fuzzy:"v2"`, `casing:"case-insensitive"` (not the default smart-case), `normalize:false` (diacritics disabled - `cafe` != `café`), `match:basicMatch` (never `extendedMatch`; the fzf-research integration trap), `sort:false` (ranking is §5/ticket 12's job). Scores are the port's, not upstream parity; tests assert selection/positions/relative-score, never exact numbers. 137 tests green. Unblocks 10 (with 06, 09) and 11 (with 09).
- [Implement the evaluator loop](issues/09-evaluator-core.md): `execute(program, symbols)` in `src/dsl/lib/evaluator.ts` (fresh empty stack, terminal-top check before each item, accumulation, dispatch by name), the `stack.ts` predicates, and `errors.ts` (`thitherError` plus `unwind(stack, error)` — the ticket's `unwind(stack)` omitted the `E` it pushes). Left as free functions; the `createInterpreter` assembly stays ticket 14's. **Type-model amendment:** the standalone `.` had nowhere to live — §1's transition table has no row for it, yet §2/§4 need the matching-portion/suffix division inside `L`. Settled by making **the separator a literal** (`Separator = readonly ["."]` joins `Literal`), so the existing `LiteralArray` carries it and `StackValue` needs no change; a tuple rather than the text `"."` so it cannot collide with the literal `..` escapes to. Splitting `L` into `{matching, suffix}` fields was rejected because `.set` still needs the *whole* list (its destination is the last literal, even past the separator). **Trap found:** the sigil alone cannot identify a stack value — accumulating the tokens `S x` builds `["S", "x"]`, which `unwind` would have mistaken for a state, so the predicates check for a plain-object body too (TypeScript also refuses to narrow a bare array in that union, and `as` is banned). An operation the environment does not bind is an `invariant`, not an `E`: `append` already rejects it at parse. 164 tests green. Unblocks 10 (with 06, 08) and 11 (with 08).


## Not yet specified

These are in scope and documented in `browser-client.md`, but not yet sharp enough to ticket: each depends on interfaces designed by an earlier stage's design ticket. They graduate into tickets when the preceding checkpoint resolves.

- **S4 — Full persistence.** Graduates after **S2 checkpoint: the skeleton is usable**. Covers the `thither.stacks.v1` and `thither.settings.v1` records, bounded history with the structural-difference rule and eviction, Web Locks serialization of read→execute→save, quota-exceeded fallback, and the malformed-data / localStorage-unavailable error states. Ticket boundaries depend on the storage interfaces drawn in **Design the client architecture and its seams**.
- **S5 — Full fallback UI and settings.** Graduates after the S4 checkpoint. Covers the 200 ms debounced live execution, highlight rendering from `hint.positions`, the `1`–`9`/`0` shortcuts, empty-target-set setup instructions, and the settings modal (history browse and restore, manual reset through `parse`, history-limit config). Expected to open with a `prototype` ticket on layout and highlight presentation.
- **S6 — Cross-browser verification and release.** Graduates after the S5 checkpoint. Covers authoring `docs/search-shortcut-checklist.md` (referenced by `browser-client.md`, currently a dangling link) and running it across browsers to verify `%s` substitution of `#`, `&`, and `+`.

## Out of scope

Ruled beyond this destination. These return only if the destination is redrawn, as a fresh effort.

- **Any client other than the browser page** — CLI, local-file adapter (ADR-0001, `browser-client.md` scope).
- **Remote or shared state, authentication, synchronization, server-side interpreter** (ADR-0001, ADR-0002); see [the state-storage research](../../docs/research/online-state-storage.md).
- **An OpenSearch description document** — conflicts with the single-file artifact and still ends in a browser prompt (`browser-client.md`).
- **Score parity with current upstream fzf** — the npm port is a pinned ordinary dependency; Thither tests its own expected behaviour (`browser-client.md`, [fzf research](../../docs/research/fzf-libraries.md)).
- **Scheme allowlisting or confirmation gates** for navigation or for `.set`/`.rm`/`.@` arriving by URL — deliberate policy, closed during charting.
