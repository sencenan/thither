# Map: Thither v1 implementation

Label: `wayfinder:map`
Tickets: `.scratch/thither-v1/issues/`

## Destination

A deployed, self-contained `index.html` on GitHub Pages that implements everything specified in [docs/dsl.md](../../docs/dsl.md) and [docs/browser-client.md](../../docs/browser-client.md), backed by a spec-derived Vitest conformance suite, and driven from a real browser search shortcut.

Done when: the language core passes conformance tests traceable to every normative rule in `dsl.md`; the client implements the full execution flow, versioned persistence, bounded history, and fallback UI of `browser-client.md`; the build emits only `index.html`; the page is live on Pages; and the shortcut has been hand-verified against the checklist.

## Notes

**This map carries execution.** Wayfinder's plan-only default is overridden: build tickets here produce working code, not just decisions. The specs are already `Status: agreed`, so the remaining unknowns are implementation-shaped.

**Domain and sources.** [CONTEXT.md](../../CONTEXT.md) is the glossary — use its terms (dimension, target, destination template, focus, match, argument balance) in ticket titles, test names, and identifiers, and avoid the terms it lists under `_Avoid_`. `docs/dsl.md` and `docs/browser-client.md` are normative. ADR-0001 (localStorage only), ADR-0002 (single-file on Pages), ADR-0003 (resume from last stack value), and ADR-0004 (publish by `v*` tag, not by merge) bind implementation choices. If a ticket's outcome contradicts a spec or ADR, say so explicitly and amend the doc or write a new ADR — never let the code silently drift from the spec.

**Skills each session should consult.** `grilling` + `domain-modeling` for any decision ticket; `codebase-design` for the interface-design tickets (04, 16); `tdd` for every build ticket; `prototype` for UI shape questions.

**Standing decisions from charting** (these are settled; don't reopen them as tickets):

- **Stage spine.** S0 Scaffold → S1 Core → S2 Walking skeleton → S3 Single-file bundle and live on Pages → S4 Full persistence → S5 Full UI → S6 Cross-browser verification and release. Deployment lands early, at S3, because ADR-0002's single-file constraint and the `%s` shortcut contract are only truly testable on a deployed page.
- **Every stage ends with a HITL checkpoint ticket** that blocks the next stage. Checkpoints exist especially where a new interface was designed.
- **Test convention.** Table-driven fixture suites whose cases name the `dsl.md` section they enforce (e.g. `§4.4 inferred separator: longest matching prefix`), plus the worked programs of `dsl.md` §7 as golden end-to-end cases. No separate traceability matrix — it would rot.
- **Types before tests.** The core's TypeScript value model is designed and written down (ticket 04) before the first core test is written.
- **Scheme policy is closed.** No client-side scheme allowlist and no scheme-based confirmation, exactly as `browser-client.md` documents. Thither is a power-user tool whose users author their own targets; `javascript:` destinations are accepted. Not revisited at S3.

**Ticket protocol.** One ticket per session. Set `Status: claimed` before any work. Resolve by appending an `## Answer` section, setting `Status: resolved`, and adding a one-line gist to Decisions so far below.

## Decisions so far

<!-- one line per resolved ticket: gist + link -->

- [Code principles and style rules](issues/01-code-principles-and-style-rules.md): Biome (2 spaces, width 100) for format and lint, strict tsc without `erasableSyntaxOnly` so enums stay, `type`/`interface` over classes, `src/dsl` + `src/lib` + `src/client` with a DOM-free tsconfig for the core and dependency-cruiser holding the import direction, values-not-exceptions in the core behind one `invariant()`, `readonly` plus test-only deep freeze, tests in `tests/` subfolders citing spec sections, `ticket/NN-slug` branches merged `--no-ff`. Written to [docs/code-standards.md](../../docs/code-standards.md).
- [S0 checkpoint: the harness is trustworthy](issues/03-s0-checkpoint.md): harness confirmed green for S1. Kept the bleeding-edge majors (TS7/Vite8/Vitest5). Moved the pi coding agent out of `package.json` into `devenv.nix` `enterShell` (repo-local pnpm global prefix) so it lives in the dev shell but CI never pulls it — `--no-optional` was rejected because it would also drop `@swc/*` native binaries `lint:boundaries` needs. Pre-commit gate kept as-is. Amended [docs/code-standards.md](../../docs/code-standards.md) with a "How the boundaries are enforced" note (two-tsconfig DOM-free mechanism + `@swc/core` parser workaround). CI pnpm-version resolution (`devEngines` set but no top-level `packageManager` field) deferred to the human to verify on a real runner.
- [Scaffold the toolchain](issues/02-toolchain-scaffold.md): pnpm project on exactly-pinned Vite 8 / Vitest 5 / TypeScript 7 / Biome 2.5 / dependency-cruiser 18 / `fzf@0.5.2`, `src/{dsl,lib,client}` with the four-symbol `src/dsl/index.ts` and the `invariant()` helper, two tsconfigs (the dsl one DOM-free), Husky + lint-staged and a CI workflow running `check → typecheck → lint:boundaries → test → build`, plus a Pages deploy workflow authored but not enabled, triggered by a `v*` tag rather than a merge ([ADR 0004](../../docs/adr/0004-publish-by-tag.md)). `@swc/core` was added as dependency-cruiser's TypeScript parser, since under TypeScript 7 it cruised zero modules and passed vacuously. DOM-free core, both boundary rules (runtime *and* type-only imports), and a deliberate type error were each observed failing and then passing.
- [Design the core type model and its seams](issues/04-core-type-model-and-seams.md): committed the whole type algebra to `src/dsl/types.ts` — `Dim`/`NormDim` and `Template`/`Url` branded (normalization is positional; `Url ⊆ Template`), envelopes wire-shaped (`["sigil", body]`, in-memory === persisted JSON), `Program` one flat sigil-tuple union, unknown-field preservation narrowed to `E` only. **Redesigned the public interface** ([ADR 0005](../../docs/adr/0005-extensible-interpreter-environment.md)): the operation set is a client-extensible `InterpreterEnv`, `defaultEnv()` → `createInterpreter(env)` → an interpreter carrying `emptyProgram`/`initialStack`/`append`/`execute`; operations are `(stack) => stack` mutating in place, dispatched by name via `env.symbols` (the extensibility *and* parallelism seam); `parse`→`append`, free `initialStack()` dropped. Amended `dsl.md` §2, `browser-client.md`, `code-standards.md`. Recorded the module/seam map and parallel waves (05/06/08 → 07 → 09 → 10/11 → 12/13). Re-wired the error/unwind primitives from 13 into 09 (operations fail *through* them); updated tickets 05/09/13/14.
- [Parse string tokens into values](issues/05-token-parsing.md): implemented the total string-token half of `append` as `parseToken(token, operations)` in `src/dsl/lib/token-parsing.ts` (type-only import of `types.ts`, a DOM-free leaf). First-match decision order settles totality: standalone `.` → separator; `..`-prefix → strip exactly one dot to a literal, never re-interpreted (`..$`→literal `.$`, `..`→`.`, `...`→`..`); single leading dot → `["op", name]` iff the env's operation set binds it, else a locally-constructed `parse_error` `E`; everything else → a verbatim literal (`node.js`, URLs, `{}` templates accumulate like dimensions). The `..$`-vs-`.$` distinction is carried by the position-0 sigil (`lit` vs `op`), which is what the ticket-09 lifecycle check reads. Literals minted as raw `Dim` here; destination validation is deliberately deferred to ticket 06. Recognition is env-relative (ADR-0005), proven with empty/extended operation sets. 44 tests green; `verify` clean; unblocks 09 jointly with 07.
- [Validate URLs and destination templates](issues/06-destination-validation.md): implemented the `destination` seam as `src/dsl/lib/destination.ts` - `validateDestination(raw)` renders every `{}` to the probe `thither` then `new URL(...)`; a successful parse *is* dsl.md §2's "parses + explicit scheme" check (the single-arg WHATWG constructor rejects relative refs), and on success the **byte-identical original** is returned branded, error path left to the caller (`.set` -> `invalid_destination`, supplied `S` -> parse-time `E`). `countPlaceholders` owns the count `P` for argDelta. **Design point to note:** the core omits the DOM lib, so `URL`'s type is supplied by a *module-scoped* `declare const URL` - the WHATWG `URL` is a universal platform global, not a client-only API, so this doesn't breach ADR-0001 and keeps localStorage/document/window still type-errors; recorded as a standing rule in `docs/code-standards.md` (universal globals re-supplied via a module-scoped `declare const`, never by widening the core `lib`). 74 tests green; boundaries clean; unblocks 07 and feeds 10/12.
- [Validate and normalize supplied structured values](issues/07-structured-value-validation.md): implemented the structured-value half of parse in `src/dsl/lib/dimension.ts` (renamed from the `normalization` module now that it validates too; dimension **validation and normalization split as separate concerns** so normalization can't fail: `isValidDimension` is the parser's whitespace gate; `normalizeDimension`/`normalizeDimensions` are total transforms - trim -> locale-independent lowercase -> dedupe -> UTF-16 sort, diacritics preserved; its own file so ops 10/11 import it alone) and `src/dsl/lib/structured-value.ts` (`validateStructured` dispatching on the `["S"|"R"|"E", body]` sigil). Every parse-time rejection is `parse_error` (§6 phase rule); a valid supplied `E` returns as itself. `S` fully validated/normalized (reuses ticket 06 for destinations, canonical-JSON key for the unique-dimension-set rule, target order preserved, unknown fields dropped); `E` held to §6's weaker rule (string type/description, type unchecked, extra fields preserved); `R` **shape-checked only** - no normalization, no destination re-validation, inputs verbatim - because a produced `R` holds partially-rendered destinations (§5). **Two spec-alignment fixes this session:** `ErrorType` corrected to §6's four types (separate commit `fix: align ErrorType…`, dropping the stale `invalid_value`/`missing_dimensions`/`invalid_stack`), and ticket 07's body reworded off `invalid_value` onto the phase rule. 119 tests green; boundaries clean. Unblocks 09 (with 05).
- [Build the fuzzy matcher adapter](issues/08-matcher-adapter.md): implemented `src/dsl/lib/matcher.ts` over pinned `fzf@0.5.2` - `match(targets, queryDimensions: readonly Dim[])` matches each dimension independently against each target's space-joined searchable string, keeps targets matched by *every* dimension (AND), sums scores, unions positions (ascending/deduped), returns in target-set order. **Seam refined in review:** the matcher takes a single flat query list and has **no `focus` param** (matching has no notion of focus; combining focus with the explicit literals and the focus-vs-explicit distinction move up to the operations, tickets 10/11) and **normalizes its own input** via the normalization seam (idempotent, so callers may pass raw `Dim` or already-normalized dims freely) - this refines ticket 04's seam row so the matcher owns *matching*, not query construction. One `Fzf` per dimension over target *objects* (selector, not raw strings, so shared searchable strings don't collide); empty query short-circuits to all-targets/score-0/no-positions. **Explicit config, justified on the ticket:** `fuzzy:"v2"`, `casing:"case-insensitive"` (not the default smart-case), `normalize:false` (diacritics disabled - `cafe` != `café`), `match:basicMatch` (never `extendedMatch`; the fzf-research integration trap), `sort:false` (ranking is §5/ticket 12's job). Scores are the port's, not upstream parity; tests assert selection/positions/relative-score, never exact numbers. 137 tests green. Unblocks 10 (with 06, 09) and 11 (with 09).
- [Implement the evaluator loop](issues/09-evaluator-core.md): `execute(program, symbols)` in `src/dsl/lib/evaluator.ts` (fresh empty stack, terminal-top check before each item, accumulation, dispatch by name), the `stack.ts` predicates, and `errors.ts` (`thitherError` plus `unwind(stack, error)` — the ticket's `unwind(stack)` omitted the `E` it pushes). Left as free functions; the `createInterpreter` assembly stays ticket 14's. **Type-model amendment:** the standalone `.` had nowhere to live — §1's transition table has no row for it, yet §2/§4 need the matching-portion/suffix division inside `L`. Settled by making **the separator a literal** (`Separator = readonly ["."]` joins `Literal`), so the existing `LiteralArray` carries it and `StackValue` needs no change; a tuple rather than the text `"."` so it cannot collide with the literal `..` escapes to. Splitting `L` into `{matching, suffix}` fields was rejected because `.set` still needs the *whole* list (its destination is the last literal, even past the separator). **Trap found:** the sigil alone cannot identify a stack value — accumulating the tokens `S x` builds `["S", "x"]`, which `unwind` would have mistaken for a state, so the predicates check for a plain-object body too (TypeScript also refuses to narrow a bare array in that union, and `as` is banned). An operation the environment does not bind is an `invariant`, not an `E`: `append` already rejects it at parse. 164 tests green. Unblocks 10 (with 06, 08) and 11 (with 08).
- **Matcher folded into its callers; standalone matcher ticket deleted** (branch `human-rewrite`): re-charting decision, no code. A matcher built without a caller only guesses its interface — the ticket-08 mistake, whose speculative adapter the human rewrite then dropped. The matcher has two callers with divergent needs: `.set`/`.rm` want selection + target-set **slot** only (no score, no positions), while `.$` alone needs the longest-prefix probe plus `score`/`positions`. So [Fuzzy matcher, in the core](issues/) was **deleted**, and the matcher is grown across its real callers instead: born in [Implement .set, .rm, and .@](issues/12-mutating-operations.md) as slot-selection (owning §3's AND / order-independence / empty-query-all fixtures), then generalized in [Implement .$](issues/13-search-render-order.md) for the prefix probe + score/positions (owning §3's scoring / boundary-spanning-positions fixtures). Both re-blocked off the deleted ticket: 12 now `Blocked by: 11`, 13 now `Blocked by: 11, 12`. Revised parallel wave: **11 → 12 → 13**.
- [Finish the evaluator loop: the terminal seal](issues/11-evaluator-loop-completion.md): completed `interpreter.ts` as a **terminal seal**, not an unwind step. **Unwind dropped** — the unwind-to-nearest-`S` shape is a consequence of each operation being **peek-not-pop** (peek `S`, consume only the transient `L`, push `E` on failure — `[K,S,E]` falls out for free), so `unwind`/`isState`/not-rolled-back move to 12/13 as the operation contract. The seal lives in one **private `push`** owning both rules for what a value does to the stack top: once `R`/`E` is on top the stack is sealed (nothing further lands), and a literal merges into the `L` on top or starts a new one. The loop routes literals and `S`/`R`/`E` through `push` and runs operations **unconditionally** — a trailing `.$` on `[S, E]` runs, finds no operand, and its `E'` is absorbed. No `break`, whole program consumed. Tested through `pushToken`/`execute` only. Landed with the human's broader core refactor (`utils.ts`, `check`—`lint` scripts, `verify` fixed).
- **Core consolidated and de-branded** (branch `human-rewrite`): the core was rewritten by hand from the six-file, branded design of tickets 04–09 into three files — `types.ts`, `parser.ts` (folds token parsing, destination validation, and structured-value validation + normalization), and `interpreter.ts` (the evaluator loop). Branded `Dim`/`NormDim`/`Template`/`Url` became plain string aliases (no `as` minting); escaping is now stored verbatim and resolved on use ([dsl.md](../../docs/dsl.md) §2); a supplied `E`'s `type` is validated against the vocabulary ([dsl.md](../../docs/dsl.md) §6, was unchecked); `ErrorTypes` gained `missing_operation`/`unknown_error`; and the matcher's code (ticket 08) was **dropped**. The public surface partly reverted: `pushToken` (mutating) instead of `append`, no `emptyProgram`/`initialStack` yet. Spec-based tests for `parser`/`interpreter` were added. This **supersedes the open core tickets 10–14**, which were deleted and re-charted as new Fuzzy matcher, in the core (ticket 10, since deleted — see the next decision), [Finish the evaluator loop](issues/11-evaluator-loop-completion.md), [Implement .set, .rm, and .@](issues/12-mutating-operations.md), [Implement .$: search, rendering, and match ordering](issues/13-search-render-order.md), and [Assemble the public surface and golden §7 programs](issues/14-public-surface-and-golden.md).
- [Implement .set, .rm, and .@](issues/12-mutating-operations.md): all three state-changing operations landed on `main` (merge `9335328`) as `src/dsl/operations/{set,rm,at}.ts`, 94 tests green. Operations **peek their state and pop only their literal array**, failing through the seal-aware `push`, so §6 unwinding and sealed-stack absorption fall out of the shape. **The matcher was born as `selectTargets` in `src/dsl/selector.ts`**: one `Fzf` over target objects on **fzf defaults**, fed the normalized query as **one space-joined pattern** — this **supersedes the ticket-08 fzf config** quoted in the Decisions above (`normalize:false`, `basicMatch`, `sort:false`, per-dimension AND). Three `dsl.md` amendments, each pinned by a fixture: §3 one-pattern matching, diacritic-insensitive; §4.1/§6 the last literal is always the destination (`S company git .set` → `invalid_destination`); §5 positions/score are the matcher's. Standards: the spec-header comment rule dropped; `src/dsl/operations/` documented and cruiser-fenced; **the DOM-free core check silently lost in `bff4055` restored** (`tsconfig.dsl.json`, two-pass `typecheck`, `declare const URL` in `utils.ts`). `defaultEnv`/`index.ts` wiring and §7 goldens stay with 14. Unblocks 13.
- [Implement .$: search, rendering, and match ordering](issues/13-search-render-order.md): `.$` landed on branch `ticket/13-search-render-order` (`b984275`) as `src/dsl/operations/search.ts`, 22 fixtures, 116 tests green. Peek-state/pop-`L` like ticket 12; any other top-of-stack shape is restored untouched and fails `missing_operand`, absorbed on a sealed stack. **Boundary inference (§4.4):** separator → matching portion in full, suffix verbatim as args; no separator → longest matching prefix, rest args; no prefix matches → empty matches but `R.inputs` reports the whole attempt. `R.inputs` is always verbatim (case and escape marks kept). **Rendering (§5):** `{}` filled by slicing (never `String.replace`, so `$` in an argument is inert); args substituted verbatim with no `resolveEscape` — flagged for revisit if a `..`-arg case ever matters. **Ordering (§5):** 3-key comparator (nonneg first, `|argDelta|` asc, score desc); the 4th key falls out of **`sort: false` on fzf** (selections arrive in target-set order) plus stable sort — no index tracking, order total by construction. This narrowly supersedes ticket 12's "fzf defaults" for `sort` only; a probe confirmed `sort` changes result order alone. **Matcher's final shape:** one `searchTargets(targets, query): Selection[]` (`{target, score, positions}`); `.set`/`.rm` project `.map((s) => s.target)` at the call site rather than via a second slot-only function. Unblocks 14.
- [Assemble the public surface and golden §7 programs](issues/14-public-surface-and-golden.md): `src/dsl/index.ts` exports `createInterpreter`, `defaultEnv` (new root file `env.ts` wiring `.set`/`.rm`/`.@`/`.$`), `emptyState`, and the type algebra. **Surface reconciled toward the code**: `pushToken` kept (mutating, returns the program; empty program is `[]`, so `emptyProgram` dropped), and **`execute(program, stack = [])` gained a shallow-copied stack argument** so resumption is explicit — [ADR 0006](../../docs/adr/0006-clients-resume-by-executing-on-the-persisted-stack.md) supersedes ADR 0003 (program carries only input tokens; `[A, B]` resumes to `[A, B']`). `initialStack` replaced by `emptyState()` because an empty stack is not a usable seed (`.set`/`.$` with no `S` are `missing_operand`). **Handed to 16:** `execute` does not re-validate the persisted stack, so detecting a corrupted record is now a client design decision. Five §7 goldens exact through the barrel; totality property via a seeded generator over a 36-item alphabet (no new dependency). `browser-client.md`, ADR 0005, `dsl.md` §2, `code-standards.md` amended in the same commit. 123 tests green. Unblocks 15.
- [S1 checkpoint: the core is correct](issues/15-s1-checkpoint.md): **signed off** after a walkthrough and live exercise in a purpose-built REPL (`pnpm repl`). Seven corrections landed (`55be66b`): `.$` resolves escapes in rendered args; `execute` copies the program; whitespace/empty string tokens are `parse_error`; Baseline 2024 recorded as the platform floor; `.set` superset behaviour and `.rm` breadth accepted and pinned. **Matching redesigned — supersedes ticket 12's one-pattern rule:** fzf `extendedMatch` with its operators (`|`, `!`, `'`, `^`, `$`) adopted into the language (`dsl.md` §2 "Search operators", `CONTEXT.md` **Search operator**), legal in `.$`/`.rm` queries only; `.set`/`.@` refuse them with the new `invalid_dimension` error and supplied `S` rejects them, so stored dimensions stay plain text and the target-set uniqueness invariant holds; operator-only tokens are `parse_error`; casing is smart-case by explicit choice (uppercase = deliberate case-sensitivity; §7 golden lowercased). 60-case operator matrix in `operators.test.ts`; 123 → 217 tests. Tooling: `scripts/repl.ts` + `scripts/tsconfig.json` + third typecheck pass, `.ts` extensions restored on all core imports (Node needs them; Vite/tsc already fine), `node_modules` relinked to the repo-local store. Handed to 16: never auto-navigate after a mutation. Unblocks 16.
- [Design the client architecture and its seams](issues/16-client-architecture-and-seams.md): **the core appends nothing; hosts compose the program** ([ADR 0007](../../docs/adr/0007-hosts-compose-the-program.md), supersedes ADR 0006). The browser client registers three host operations on the ADR-0005 environment and every run is `.load <tokens> .$ .out .save`: `.load` reads/validates (via `pushToken`) the persisted stack or pushes `emptyState()`; `.out` pops an `R`/`E` (only) into a client-held **output register**; `.save` persists the stack as-is (never empty) and records `changed`. The client reads only the register, never the returned stack. No storage interface: the Web Lock wraps the one synchronous `execute` call; malformed data is `.load`'s `parse_error` sealing the stack; save failure is `unknown_error`; `localStorage` unavailable throws, caught once in `main.ts`. `dsl.md` §6 stays closed. Module map: `input.ts`, `persistence.ts`, `session.ts` (latch + pure `decideNavigation`, refuses auto-navigation when `changed`), `view.ts` (happy-dom tests), `main.ts` (untested root). `location.replace` for auto-navigation. `dsl.md` §1/§4.2/§7, `browser-client.md`, ADR 0005, `CONTEXT.md` (Host, Host operation, Output register) amended. New tickets [Remove the implicit terminal from the core](issues/24-remove-implicit-terminal.md) (core + tests) and [The REPL composes its own program](issues/25-repl-epilogue.md) (blocked by 24); 18/19/20 rewritten off stale ADR-0003 vocabulary. **Sequencing: core → REPL → client** — 17 and 18 are both blocked by 25, so the only frontier ticket now is 24.
- [Remove the implicit terminal from the core](issues/24-remove-implicit-terminal.md): `execute` now evaluates exactly the program given (ADR 0007) — deleted the `TERM_OP` append/`hasTerminalOp`/program-copy from `interpreter.ts`, dropped `TERM_OP` from `types.ts`, and inlined `'.$'` in `env.ts`. Tests close their programs with an explicit `.$` where they relied on the implicit one (search's `run` and each §7 golden; the two `.$` cases in `operators.test.ts`), and the harness's no-op identity `.$` is gone. The `..$` assertion was corrected to accumulate **verbatim** (`['L', ['..$']]`, resolved on use). `pnpm verify` green, 217 tests. The **REPL is knowingly left broken** (`scripts/repl.ts` composes no `.$`) until ticket 25. Unblocks 25.
- [The REPL composes its own program](issues/25-repl-epilogue.md): `scripts/repl.ts` `execute` now composes `[...tokens, '.$']` as a host (ADR 0007), the empty line composing `['.$']` so it still searches on focus. The §7 goldens already end in an explicit `.$`, so the extra `.$` is absorbed on the sealed stack and results are unchanged (verified live). Dropped the implicit-terminal detection/display and `showTop`'s `implicitTerminal` param; instead **echoes the composed program dim** (`> … .$`) so the epilogue is visible when teaching. Both comments crediting the interpreter with appending `.$` removed (`grep 'appends'` empty). No `.load`/`.out`/`.save`; resumes through `execute(program, stack)`. Landed on `main` (`26d1c6a`); 217 tests green. **Unblocks 17 and 18** — the frontier now opens onto the S2 walking skeleton.
- [Read program input from the URL](issues/17-url-input-reading.md): `src/client/input.ts` `readInput(url)` selects program input by `q`-**presence** (`URLSearchParams.has`) — query `q` (an empty `?q=` counts and wins) → fragment `q` (leading `#` stripped) → `[]` — decodes **once** through `URLSearchParams` (`+`→space, `%2B`→literal `+`, first `q` wins), then splits on `/\s+/` dropping empties. A pure function over the full URL string (WHATWG `URL`, no DOM), so it tests in plain Node; `main.ts` will feed it `location.href`. 17 fixtures, 234 tests, `pnpm verify` green. Committed on `ticket/17-url-input-reading` (`b13a9e8`); **`--no-ff` merge to `main` pending a human** (git guardrail). Unblocks 19 jointly with 18.
- [Register the host operations: `.load`, `.out`, `.save` (naively)](issues/18-naive-persistence.md): the client's whole persistence landed as `src/client/browser-env.ts` (sole namer of `thither.stacks.v1`), 8 fixtures run through the interpreter against a `Map`-backed storage fake, 242 tests green. Two review turns reshaped the seam: **operations now receive the interpreter as their first argument** (`OpFn = (interp, stack) => stack`, amending [ADR 0005](../../docs/adr/0005-extensible-interpreter-environment.md)) so `.load` reaches `pushToken` with no thunk — the four language ops ignore it (`_interp`); and **`createBrowserEnv(storage): BrowserEnv`** mirrors `defaultEnv`, returning its four symbols plus the three host ops, with `BrowserEnv extends InterpreterEnv, OutputRegister` so the register fields (`terminal`/`loaded`/`saved`) are splatted onto the env (one value, no separate persistence object). `.load` seeds `emptyState()`/validates the stored current stack and pushes canonical values (malformed record or first `E`-parsing value seals with `parse_error`, `loaded: false`); `.out` pops only an `R`/`E` into `env.terminal`; `.save` writes the single-element record and records structural `changed` (absent = `[emptyState()]`), nothing on an empty stack — the sealed `E` is captured by `.out`, leaving a bad record untouched. No history/eviction/quota/`settings.v1` (S4). Unblocks 19 (with 17).
- [Wire the skeleton: execute, navigate, or list](issues/19-skeleton-execute-and-navigate.md): the S2 walking skeleton landed as `src/client/{session,view}.ts` + a rewritten `main.ts`, 45 new tests (262 total), `pnpm verify` green. **`session.ts`** composes `.load <tokens> .$ .out .save`, executes it inside `navigator.locks` (or directly when absent), reads the **register only**, and holds the `navigationArmed` latch; **`decideNavigation(register, armed)`** is pure/table-tested (armed + single `R` match with `argDelta >= 0` + `saved.changed === false`) — narrows `saved` with `'changed' in saved` because `Array.isArray` doesn't remove the readonly-tuple `ThitherError` under TS7. **`view.ts`** renders `R.matches` as links in emitted order (no client sort), an `E` as `type: description`, and the Settings-reset hint when `loaded` is false. **`main.ts`** (untested by design) is the composition root with the client's one `try/catch` for an unavailable localStorage. `run(input, mode)` gates navigation on `mode === 'initial'` so `'live'` is an S5 caller, not a new param. **Empty input never auto-navigates** (found in the dev-server check — an empty `.$` matches every target, so a one-target state would redirect on a blank open and never be reachable): navigation also requires `input.length > 0`, amending `browser-client.md` step 4. An **incomplete single match** (`argDelta < 0`) is never auto-navigated to, but is still rendered as a clickable link — "non-navigable" governs only the auto/shortcut path. **Added `happy-dom` as a devDependency** (ticket 16 anticipated it) for `view.test.ts` via a per-file `@vitest-environment` pragma; the default vitest env stays `node`. Unblocks 20 (S2 checkpoint).
- [`.set` reads the destination as its first literal](issues/26-set-url-first.md): usability flip so a shortcut leads with the browser URL and appends dimensions after it (`S https://github.com/company/{} company git .set`). `src/dsl/operations/set.ts` shifts instead of popping; the first literal is the destination unconditionally, so `.set` never searches forward for a URL and a leading non-URL literal is `invalid_destination`. Amended `dsl.md` §4.1/§6/§7 and the `.set`/golden/operators/at tests, plus the REPL usage comment and `browser-client.md`'s `.set` example. `pnpm verify` green, 217 tests, verified live in the REPL. Landed on `main`.
- **Targets carry one variant per arity; `.set` is exact-key and ignores focus** ([ADR 0008](../../docs/adr/0008-arity-keyed-variants-and-exact-key-set.md)): grilling session, no code. A target is now a **key** (normalized dims joined) owning **variants** — templates distinguished by **arity** — so `jira` and `jira/{}` are one target; `.$` emits one match when a variant's arity equals the argument count (**best fit**) else one match per variant, so direct navigation stays "exactly one match, Δ ≥ 0"; ordering is score → matcher order → within a target `0`/`+`/`−`; `.set` does exact key lookup with no fzf and no focus (`ambiguous_set` dropped); `.rm` keeps fzf + focus and a separator's suffix *count* names the arity to remove; focus may now carry operators. `CONTEXT.md` gained Key/Variant/Arity/Best fit (Searchable string retired). Tickets: [27 spec](issues/27-variants-spec-update.md) → [28 implementation](issues/28-variants-implementation.md).
- [Implement arity-keyed variants, exact-key `.set`, arity-targeted `.rm`](issues/28-variants-implementation.md): [ADR 0008](../../docs/adr/0008-arity-keyed-variants-and-exact-key-set.md) landed across core, client, and REPL; `pnpm verify` green (273 tests) with the new §7 **Variants of one target** golden and the REPL walk verified live. `Target` is now `[key, variants]`, `State.targets` a keyed `TargetSet` object (exported in place of `Target`, which became an internal selector entry pair and left the barrel), `Match` slot 2 the key string, `ambiguous_set` gone; `keyOf`/`arityOf` moved into `utils.ts`. **`.set` dropped fzf** — exact key, focus-blind, insert / replace-by-arity / add-variant. **`.rm` keeps fzf + focus** — no separator removes whole targets, a separator's suffix *length* names the arity to remove (emptied targets dropped). **`.@` stores focus verbatim and now accepts operators**; parser stores focus verbatim too (operators legal, operator-only/whitespace/empty rejected) and re-normalizes keys (duplicate-after-normalization / operator-key / arity-collision → `parse_error`, variants sorted arity-ascending). **`.$` fans out per target** (best fit → one match, else one per variant) with ordering score-desc across targets (ties → target-set = object iteration order) then `0`/`+`/`−` within a target, rows grouped contiguous. Client: `view.ts` renders the key; `decideNavigation` unchanged (fixture pins two-variant no-nav); no persistence migration — old-shape records parse to `parse_error` and reset by hand (fixture). Found while implementing: `.$` prefix inference fuzzy-matches argument letters, so variant tests pin the boundary with an explicit `.` (no spec change). **This completes the variants effort (26 → 27 → 28).**
- [S2 checkpoint: the skeleton is usable](issues/20-s2-checkpoint.md): **signed off** — the human drove the walking skeleton on the dev server (URL `?q=`-driven, since the live input box is S5) through set / incomplete-match / complete-match auto-nav / ambiguous / no-match / error / reload-replay / corrupted-record, all as specified. Flow feels right; the client stayed dumb (register only, no repair — a hand-corrupted record sealed with `parse_error` and was left untouched). Three rough edges accepted: never auto-navigate to an unfilled `{}`, the reset hint promising an S5-only Settings control is fine for the skeleton, and the all-targets list after `.set` is welcome. `.load`/`.out`/`.save` are sound as the S4 seam — nothing to tear out. **The Web Lock, malformed-data, and localStorage-unavailable S4 items were already done/exercised by the skeleton**, so the S4 fog graduated into just four build tickets plus a checkpoint: [Introduce `thither.settings.v1` and the history limit](issues/29-settings-and-history-limit.md) → [Bounded history and eviction inside `.save`](issues/30-bounded-history-and-eviction.md) → ([Quota-exceeded retry inside `.save`](issues/31-quota-exceeded-retry.md), [History readers: `history()`, `restore()`, `reset()`](issues/32-history-readers.md)) → [S4 checkpoint: persistence is durable](issues/33-s4-checkpoint.md).
- [Amend `dsl.md` for arity-keyed variants and exact-key `.set`](issues/27-variants-spec-update.md): `docs/dsl.md` §1–§7 and `browser-client.md` now specify ADR 0008 in glossary vocabulary — `T` is `{ key: [variants] }`, `m` carries the key, `.set` is exact-key/focus-blind with a three-row insert/replace/add table, `.rm` removes whole targets or the variant whose arity is the suffix length, `.$` emits one match on a best fit else one per variant, ordering is score → target-set order → `0`/`+`/`−` within a target, `ambiguous_set` is gone. **One gap the ADR left open was pinned:** focus is **no longer normalized at all** — stored verbatim like any literal array (typed order, case, escape form), since `|` binds by position now that focus may carry operators. New §7 golden "Variants of one target". Docs only; `pnpm verify` green. Unblocks 28.

- [Bundle to a single self-contained index.html](issues/21-single-file-bundle.md): `pnpm build` emits exactly one file, `dist/index.html`, with the app's own JS/CSS (incl. `fzf`) inlined into one `<script type="module">` — the Vite config was already single-file-shaped, so this ticket added the **verification** the plugin can't guarantee. **Single-file means deployment, not runtime isolation:** the check enforces (1) `dist/` holds only `index.html` and (2) no *local* (relative/root-relative) asset ref, but **allows external/CDN/protocol-relative URLs** (public fonts, fonticons) since the loaded page may fetch resources it can reasonably expect — Thither is a network-facing redirector. `scripts/verify-single-file.ts` (pure `findLocalAssetReferences`/`verifyDist` + a `closeBundle` Vite plugin that throws) fails `vite build` if a local asset survives or an extra file lands in `dist/`; it can't tell a CDN font from a CDN library, so `browser-client.md`'s bundle-don't-CDN rule stays review discipline. Proven by `scripts/tests/verify-single-file.test.ts` (11 cases, now run via `vitest.config.ts` include extended to `scripts/**/tests/`) and live — dropping `viteSingleFile()` failed the build with exit 1. `base: './'` + inlined module = no local fetch, so file:// is portable (booted from a `file://` URL in happy-dom, `#app` present); true rendered file:// smoke deferred to the S3 checkpoint (no headless browser here). 285 tests green. Landed on `ticket/21-single-file-bundle`; `--no-ff` merge pending human.

- [Enable GitHub Pages and deploy](issues/22-enable-pages-and-deploy.md): **live at `https://sencenan.github.io/thither/`** (shortcuts `?q=%s` and `#q=%s`), published by the `v1.0.0` tag (ADR-0004 confirmed: no merge publishes), serving the byte-identical single-file `index.html` and verified executing end to end in headless Chrome (`?q=example` → `.load example .$ .out .save` → rendered register). Three deploy assumptions were wrong and fixed on `main` before the tag stuck: (1) **added top-level `"packageManager": "pnpm@11.27.1"`** so `pnpm/action-setup` resolves a version — this closes the S0 checkpoint's deferred CI-pnpm question; (2) `ci.yml` called a stale `pnpm run check`, repointed to `pnpm run lint`; (3) the `github-pages` environment allowed only branch `main`, so added a `v*` **tag** deployment-branch policy (repo config, so future `v*` releases just work). Pages was already enabled (GitHub Actions source). Unblocks the S3 checkpoint.

- [S3 checkpoint: dogfood the live shortcut](issues/23-s3-checkpoint.md): **signed off** — the human drove the deployed Thither from their real address bar (both `?q=%s` and `#q=%s`, real targets, arguments, `%s` stress cases). Shortcut contract holds, single-file behaves on Pages as ADR-0002 assumed, fallback tolerable through S4/S5, scheme policy left closed. **One rule change fell out** (`grilling`): `<u> a b .set a b` didn't redirect because the documented "never auto-navigate after a mutation" `changed` guard suppressed it — the human ruled the spec wrong (*let the user do what the program says*). Decided to replace `changed` with a **query-driven rule**: navigate iff one match, `argDelta ≥ 0`, **and `R.inputs.length > 0`** (verified live to redirect the reported case, keep `home .set` showing the list, subsume the blank-open guard, and leave focus-only searches non-navigating). Filed as [Auto-navigate on a non-empty query, drop the `changed` guard](issues/34-navigate-on-nonempty-query.md) (code + `browser-client.md` + new ADR); ticket 30 reworded so its structural-diff is history-internal. `%s` handling behaved as expected — the query-form `+`→space / `#`-truncation is the S6 checklist seed.

- [Auto-navigate on a non-empty query, drop the `changed` guard](issues/34-navigate-on-nonempty-query.md): [ADR 0009](../../docs/adr/0009-auto-navigate-on-a-non-empty-query.md) — `decideNavigation(register, armed): boolean` became `resolveNavigationDestination(register): Promise<string>` in a new `src/client/navigation.ts`, resolving to the single match's rendered destination iff `R && one match && argDelta ≥ 0 && R.inputs.length > 0`, nothing else, so the caller no longer re-narrows the terminal. Three review simplifications rode along: the `input.length > 0` term in `run` went (a duplicate statement of the rule; blank-open fixture still pins it); `run`'s `'initial' | 'live'` mode parameter went (redundant with the latch); the `navigationArmed` latch went (`run` had one caller, once at load), and with no state left **`session.ts` was collapsed into `main.ts`** (the split had become a pure test seam; the human rewrote `main.ts` by hand as a promise chain — review caught and fixed a TDZ `ReferenceError` and a too-wide `.catch` that swallowed the localStorage throw, then boot-probed it under happy-dom; `main.ts` stays untested by design and the rule's table + end-to-end fixtures live in `navigation.test.ts`) — S5's live execution owns "no auto-navigation once the UI is shown" (see S5 fog). **`OutputRegister.saved` deleted outright** (with `changed` gone it had no reader) — the register is `{ terminal, loaded }`, and a failed `.save` will write its `E` into `terminal` over the captured `R`, so no-navigation-on-failed-save needs no gate. Pinned end to end: `<u> a b .set a b` navigates (set-and-go), `<u> home .set` lists (set-to-confirm), blank open / `home .@` focus-only show the page, incomplete single match renders but never navigates. `browser-client.md` step 4 + `.save` bullet, ADR 0007's `.save` bullet, and `CONTEXT.md` **Output register** amended. [Ticket 31](issues/31-quota-exceeded-retry.md) reworded to write the failure into `terminal`. 283 tests; landed on `ticket/34-navigate-on-nonempty-query`, `--no-ff` merge pending a human.

- [Introduce `thither.settings.v1` and the history limit](issues/29-settings-and-history-limit.md): `src/client/browser-env.ts` (moved to `persistence.ts` by ticket 30) became the sole namer of both records and exports `Settings { historyLimit }`, `isHistoryLimit(unknown)` (nonnegative integer; `0` legal), `readSettings(storage)` (absent/malformed → `{ historyLimit: 10 }`, never repairs the record, throwing storage propagates like `.load`), and `writeSettings(storage, settings)`. **Free functions over `StorageArea`, not env fields:** ticket 30's `.save` already holds `storage` inside the closure and the S5 modal holds `localStorage`, so the env stays "operation set + output register". The predicate is exported so the modal validates typed input with the reader's rule. No `.save` consumption or eviction yet (ticket 30). 11 fixtures, 303 tests, `pnpm verify` green; on `ticket/29-settings-and-history-limit`, `--no-ff` merge pending a human. **Unblocks 30.**

- [Bounded history and eviction inside `.save`](issues/30-bounded-history-and-eviction.md): `.save` now writes the oldest-first `thither.stacks.v1` record with structural-difference dedup and front eviction to `N + 1`, `N` from ticket 29's `readSettings`; callers untouched. **Review refactor: the record formats split out of `browser-env.ts` into a new root file `src/client/persistence.ts`** (ticket 16's name), now the sole namer of both keys and home of `StorageArea` + the settings functions. Its seam: `type StackHistory` (oldest-first, current last, values unvalidated), `readHistory(storage)` 

- [Strip the consumed input from the URL when the fallback page is shown](issues/35-strip-consumed-input.md): reverses `browser-client.md`'s keep-and-replay policy after dogfooding — an accidental refresh after `<u> home .set` re-ran the mutation. `stripInput(url)` in `src/client/input.ts` deletes `q` from **both** the query and the fragment (so a reload cannot fall through to the fragment), keeps every other parameter/path/`q`-less fragment verbatim, and `main.ts` applies it via `history.replaceState` on the render path only — navigation leaves the page, and the bare error page executed nothing. The tokens survive as `createBrowserEnv(storage, input)`'s `readonly input` field, so S5's text field seeds from the env, not the address bar. Reload is now a blank open; "Reading input", "Fallback UI", and ADR 0005 amended.

## Not yet specified

These are in scope and documented in `browser-client.md`, but not yet sharp enough to ticket: each depends on interfaces designed by an earlier stage's design ticket. They graduate into tickets when the preceding checkpoint resolves.

- **S5 — Full fallback UI and settings.** Graduates after the S4 checkpoint. Covers the 200 ms debounced live execution — which must never auto-navigate (`browser-client.md` "Fallback UI"); the skeleton's `navigationArmed` latch was removed in ticket 34, so live runs need their own non-navigating path (there is no session object; `main.ts` runs once and either navigates or renders) — highlight rendering from `hint.positions`, the `1`–`9`/`0` shortcuts, empty-target-set setup instructions, and the settings modal (history browse and restore, manual reset through `parse`, history-limit config). Expected to open with a `prototype` ticket on layout and highlight presentation.
- **S6 — Cross-browser verification and release.** Graduates after the S5 checkpoint. Covers authoring `docs/search-shortcut-checklist.md` (referenced by `browser-client.md`, currently a dangling link) and running it across browsers to verify `%s` substitution of `#`, `&`, and `+`.

## Out of scope

Ruled beyond this destination. These return only if the destination is redrawn, as a fresh effort.

- **Any client other than the browser page** — CLI, local-file adapter (ADR-0001, `browser-client.md` scope).
- **Remote or shared state, authentication, synchronization, server-side interpreter** (ADR-0001, ADR-0002); see [the state-storage research](../../docs/research/online-state-storage.md).
- **An OpenSearch description document** — conflicts with the single-file artifact and still ends in a browser prompt (`browser-client.md`).
- **Score parity with current upstream fzf** — the npm port is a pinned ordinary dependency; Thither tests its own expected behaviour (`browser-client.md`, [fzf research](../../docs/research/fzf-libraries.md)).
- **Scheme allowlisting or confirmation gates** for navigation or for `.set`/`.rm`/`.@` arriving by URL — deliberate policy, closed during charting.
